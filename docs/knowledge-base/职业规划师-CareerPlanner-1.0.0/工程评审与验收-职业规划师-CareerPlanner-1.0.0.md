# 验收报告：职业规划师 Career Planner 1.0.0

> 当前结论：通用能力、首周提醒和私人迁移执行已完成；迁移存在逐项记录的 DLP、HTTP 403 与格式限制，等待 PO 浏览验收。本期未进行职业分析。

## 1. 实施范围

| 阶段 | 结果 | 说明 |
|---|---|---|
| A 方案 | 通过 | Gate 1 / Gate 2 已确认 |
| B 本地知识库与职业指导 | 通过 | iCloud `career/`、空白模板与 `plan-career` Skill 已建立 |
| C 目标、计划与提醒 | 通过 | 一条 heartbeat 包含 8 月 17–21 日五次运行；周末为零 |
| D 真实材料归档 | 通过（有明确限制） | 220 个 Wiki 节点队列耗尽，正文、可获得原生导出、媒体、恢复元数据和哈希清单已保存；受限项未标成完整 |
| E 首轮咨询与四项产物 | 未开始 | 随真实材料和访谈逐日推进 |

不设 App 或前后端开发阶段。

## 2. 自动化验收

- 运行态：ACTIVE。
- 调度范围：2026-08-17、18、19、20、21，共五次。
- 调度内部时间：UTC 06:30；换算后用户当地时间为 `Asia/Shanghai 14:30`。
- 周末运行数：0；8 月 21 日后无继续触发。
- Prompt 与私人注册表 SHA-256 一致；运行态 Prompt 与规范 Prompt 逐字一致（忽略尾部换行）。
- 提醒只读取安全任务状态，不读取工作原文；包含完整输入输出议程、最低版和停止条件。

## 3. 测试证据

| 检查 | 结果 |
|---|---|
| Career Planner 聚焦测试 | 6/6 通过 |
| Career Planner + 状态工具聚焦测试 | 77 通过，1 项按设计跳过 |
| 全量 Python tools 测试 | 375 通过，1 项按设计跳过 |
| 官方 Skill `quick_validate.py` | 通过；使用隔离 `uv --with pyyaml` 运行，未修改项目依赖 |
| 项目治理检查 | 通过 |
| Git 索引与 `origin/main..HEAD` 历史隐私检查 | 通过 |
| 实际 iCloud 根的项目便携性校验 | 仅保留既有失败：私有迁移 manifest 含旧机器绝对路径；按 PO 要求未修改 |
| 私人迁移完整性校验 | 8,556 个文件零空文件，220/220 个节点恢复元数据完整，39/39 个离线抽样通过 |
| `git diff --check` | 通过 |

独立 worktree 本身不复制 `USER.md`、`GOALS.md`、`career/` 和自动化等私人真相源。验收使用当前分支校验器对实际 iCloud 根执行；私人原文库按设计排除通用源码秘密扫描，Git 隐私扫描继续独立生效，结果只剩 PO 已明确要求本任务不处理的旧机器路径问题。

## 4. 隐私与数据结果

- `career/` 已同时加入版本化 `.gitignore` 和本地 exclude；根工作区状态不显示该目录。
- Git 只包含通用 Skill、合成测试和去敏知识库，没有真实职业材料、经历内容、提醒运行标识或服务标识。
- 私人原文库现有 8,556 个已校验文件，约 2.94 GB；`career/` 在 Git 状态与全历史对象中均为零。
- 本期只完成原文保全和迁移，没有生成证据卡、职业分析、交接改写或简历内容。
- 未读取 `00-收件箱/`，未投递、共享或发布。

## 5. 私人迁移验收

| # | 完成条件 | 结果 |
|---:|---|---|
| 1 | 两个项目 Wiki 的递归节点队列耗尽 | 通过；220 个节点，父子关系与同级顺序已保存 |
| 2 | 季度 OKR 和转正述职有离线正文及可获得原生导出 | 通过；两项均有 Markdown、XML 和 DOCX |
| 3 | 图片、附件、画板和嵌入对象已下载或逐项标记限制 | 通过；3,443 个唯一对象下载，252 个无权限、19 个 DLP 限制、5 个部分 |
| 4 | 所有文件有大小和 SHA-256 | 通过；8,556 个文件、零空文件；清单因自引用不包含自身 |
| 5 | 所有节点有恢复元数据 | 通过；220/220 |
| 6 | 抽样验证导出文件可离线打开 | 通过；39/39 |
| 7 | 完整性报告未把缺失内容标成完整 | 通过；205 个来源保持 `部分`，16 个为 `完整` |
| 8 | `career/` 未出现在 Git 状态或提交历史 | 通过 |
| 9 | 本期未进行职业分析 | 通过 |
| 10 | PMO 和验收文档同步真实状态 | 通过；本文件与项目管理文档已更新 |

受限项不是迁移成功文件：`no_permission`、`restricted_dlp`、`partial` 和只保留元数据的组件均保留原状态，不计入完整内容。

## 6. PO 验收项

1. 直接浏览 `career/个人知识库/`，确认目录、Markdown 和抽样导出可读。
2. 检查 `个人知识库/00-管理/完整性报告.md`，确认 DLP、HTTP 403、部分导出和平台组件限制的处理符合预期。
3. 验收后再单独决定是否授权进入证据提炼与职业分析；本次验收不自动授权后续分析。
