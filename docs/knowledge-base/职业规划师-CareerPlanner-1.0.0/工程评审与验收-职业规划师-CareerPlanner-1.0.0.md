# 验收报告：职业规划师 Career Planner 1.0.0

> 当前结论：通用能力和首周提醒合成验收通过；PO 已授权指定工作知识库迁移，真实材料归档正在执行，尚未验收。

## 1. 实施范围

| 阶段 | 结果 | 说明 |
|---|---|---|
| A 方案 | 通过 | Gate 1 / Gate 2 已确认 |
| B 本地知识库与职业指导 | 通过 | iCloud `career/`、空白模板与 `plan-career` Skill 已建立 |
| C 目标、计划与提醒 | 通过 | 一条 heartbeat 包含 8 月 17–21 日五次运行；周末为零 |
| D 真实材料归档 | 进行中（授权阻塞） | 本地结构和原始清单快照已完成；飞书递归节点、正文和资产等待用户授权 |
| E 首轮咨询与四项产物 | 未开始 | 随真实材料和访谈逐日推进 |

不设 App 或前后端开发阶段。

## 2. 自动化验收

- 运行态：ACTIVE。
- 调度范围：2026-08-17、18、19、20、21，共五次。
- 调度内部时间：UTC 06:30；换算后用户当地时间为 `Asia/Shanghai 14:30`。
- 周末运行数：0；8 月 21 日后无继续触发。
- Prompt 与私人注册表 SHA-256 一致；运行态 Prompt 与规范 Prompt 逐字一致（忽略尾部换行）。
- 提醒只读取安全任务状态，不读取工作原文；包含完整输入输出议程、最低版和停止条件。

## 3. 测试证据

| 检查 | 结果 |
|---|---|
| Career Planner 聚焦测试 | 5/5 通过 |
| 全量 Python tools 测试 | 339 通过，1 项因私有契约不在 Git 测试夹具中而按设计跳过 |
| 官方 Skill `quick_validate.py` | 通过 |
| 项目治理检查 | 通过 |
| Git 隐私与凭据检查 | 通过 |
| 实际 iCloud 根的项目便携性校验 | 通过 |
| `git diff --check` | 通过 |

独立 worktree 本身不复制 `USER.md`、`GOALS.md`、`career/` 和自动化等私人真相源，因此直接在 worktree 运行便携性校验会按设计报告私人文件缺失；验收使用当前分支的校验器对实际 iCloud 根执行，结果通过。

## 4. 隐私与数据结果

- `career/` 已同时加入版本化 `.gitignore` 和本地 exclude；根工作区状态不显示该目录。
- Git 只包含通用 Skill、合成测试和去敏知识库，没有真实职业材料、经历内容、提醒运行标识或服务标识。
- 当前私人职业知识库只有用户已知事实、空白模板和明确标为“分析推断”的初步建议。
- 未读取 `00-收件箱/`，未导入工作文件或链接，未投递、共享或发布。

## 5. 私人迁移验收

本批迁移只有同时满足以下条件才可标记完成：

1. `career/个人知识库/` 是唯一原文真相源，旧 `career/01-原始资料/` 不再接收原文。
2. 四个业务一级类目与用户清单一致，两个项目知识库保留递归父子关系和同级顺序。
3. 每个节点记录来源、捕获时间、版本、格式、字节数、SHA-256 和完整性状态。
4. 正文、图片、附件、嵌入对象和原生导出分别校验；缺失项明确标为部分或失败。
5. `career/` 全部保持 Git 忽略，Git 变更只有去敏设计、使用和验收说明。
6. 本期没有生成证据卡、职业分析、交接改写或简历内容。

## 6. PO 验收项

1. 直接浏览 `career/`，确认目录和 Markdown 可读。
2. 核对初步职业路径的一主两辅是否适合作为下周访谈起点。
3. 检查 `个人知识库/00-管理/完整性报告.md`，确认缺失或受限项的处理符合预期。
