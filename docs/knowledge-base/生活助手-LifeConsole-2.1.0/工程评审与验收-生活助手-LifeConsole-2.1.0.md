# 工程评审与验收 - 生活助手 - Life Console - 2.1.0

> 状态：Gate 2B 已确认 / 阶段 2B-1 本地实现完成 / 阶段 A 平台 POC 仍阻塞
>
> 当前证据：本机代理核心与系统页纯合成 UI 已有测试和构建证据；Worker 导出、HTTPS 回环和端到端联调尚未执行
>
> 隐私边界：真实日记、健康数据、Sites 资源标识、机器路径、凭据和真实备份不进入 Git、PR、CI 或本文件

## 1. 工程评审结论

结论建议：**有条件通过，先做浏览器回环 POC，再进入完整实现**。

成立条件：

1. PO Gate 2 确认设计 O1-O5 与技术 Q1-Q7。
2. 当前 ChatGPT in-app Browser 和 Chrome 至少一种正式使用环境能在明确授权后安全访问回环代理。
3. 合成规模 POC 证明单请求导出不超 Worker 内存/时间；否则停止并重新评审分块一致性。
4. 本机代理在任何失败下注明并证明“上一份有效备份未被覆盖”。
5. PR #38 只提炼有效代码，不把恢复包与 R2 备份路径带入 2.1.0。

## 2. 测试分层

| 层级 | 工具 | 目标 |
|---|---|---|
| 治理与隐私 | Python / shell / Git history scan | 规范门禁、私人路径、凭据、绝对路径和真实数据不进入 Git |
| Worker 单元 | Node test / Web Crypto / D1 adapter | manifest、导出锁、状态机、错误码、审计最小化 |
| 本机代理单元 | Python unittest | ZIP 安全校验、权限、临时文件、原子替换、回执 |
| Worker 集成 | Miniflare + D1 + 合成 KEK | 全量解密导出、数量/摘要、写入锁、失败释放 |
| 前端单元 | Vitest + RTL | 单一备份卡、状态文案、移动端禁用、无恢复包/审计 UI |
| 回环 POC | 当前 in-app Browser / Chrome + 临时代理 | LNA/CORS/Origin、权限拒绝、代理离线和合成 ZIP 转发 |
| 合成 E2E | Playwright + Miniflare + 临时目录 | 网页 → Worker → localhost → 原子备份 → 云端回执 |
| 私有验收 | PO + 临时/正式环境 | 仅在后续独立授权后验证真实 Sites 和 iCloud |

## 3. Gate 2 后第一工作块：回环 POC

POC 只使用合成文本和临时目录，不绑定真实 D1、R2、KEK 或 iCloud。

### POC-01 浏览器连接

- HTTPS 测试页访问回环 `/v1/health`。
- 记录是否出现本地网络权限提示、CORS 预检及用户拒绝后的错误形态。
- 当前 ChatGPT in-app Browser 与 Chrome 分别记录；Safari 只观察，不作为首版通过条件。

### POC-02 安全边界

- 非白名单 Origin 预检失败。
- 缺失自定义意图 Header、错误 Content-Type、重定向、局域网绑定全部拒绝。
- 代理端口只监听 `127.0.0.1`，不监听 `0.0.0.0`。
- 不存在任意文件读取、目录浏览或命令执行路由。

### POC-03 合成传输

- 传输合成 ZIP，代理写入临时目录并返回去敏回执。
- 页面关闭、代理中断和重复请求不产生部分正式文件。
- POC 通过后才允许开始 W3/W4 实现；失败则回到 Gate 2 重新选型。

### 2026-08-11 阶段 A 实测记录

| 项目 | 结果 | 去敏证据与结论 |
|---|---|---|
| 回环服务安全边界 | 通过 | 4 项 Python 合成单测通过：Origin SHA-256 白名单、CORS/LNA 预检、自定义意图 Header、Content-Type、IPv4 loopback 绑定和合成 ZIP 原子写入；仅使用临时目录 |
| 当前正式 HTTPS 页面实际跨 Origin 请求 | 未通过 / 阻塞 | 当前已部署版本的 CSP 仍是 `connect-src 'self'`，且本轮禁止正式部署；浏览器自动化的只读页面上下文也不提供 `fetch`，因此不能把实际 HTTPS → localhost/LNA 兼容性冒充为已验证 |
| 下一候选源码准备 | 通过 | 未部署源码已将 CSP 最小放行固定回环端口；仍需后续单独取得候选部署授权后，在真实页面验证权限提示、拒绝形态与预检 |
| S 容量档 | 本地算法通过 | 约 1.05 MB 合成 NDJSON，归档约 1.05 MB，约 42 ms，进程 RSS 增量约 13.9 MB |
| M 容量档 | 本地算法通过 | 约 4.19 MB 合成 NDJSON，归档约 4.19 MB，约 180 ms，进程 RSS 增量约 23.1 MB |
| L 容量档 | 本地算法通过 | 约 8.39 MB 合成 NDJSON，归档约 8.39 MB，约 418 ms，进程 RSS 增量约 66.1 MB |
| Worker 平台容量 | 延期 | 上述仅证明同一 ZIP 算法在本机 Node 进程可运行，不等于 Sites Worker 的真实时间/内存上限；本轮无部署授权，不声称平台容量通过 |

阶段 A 总结：**条件未通过**。安全边界与本地容量 POC 可继续保留，但正式 HTTPS 页面回环与 Worker 平台容量仍缺实际候选环境证据。依照 Gate 2 的“先 POC、后实现”，阶段 B-E 暂不启动；正式环境、真实数据和云资源均未触碰。

### 阶段 A 私有候选部署授权

- PO 已于 2026-08-11 明确授权一次“不含真实数据的私有候选部署”，仅用于补齐阶段 A 的 HTTPS 回环和 Worker 容量验证。
- 部署复用既有独立 Owner-only 候选项目，不覆盖正式 URL，不创建重复候选项目。
- 候选构建只暴露合成 POC API 与静态合成投影；不声明或绑定 D1、R2、KEK，不读取 iCloud。
- 本授权不扩大到正式部署、真实数据读取或备份、真相源切换、资源删除、阶段 B-E 实现或 PR 合并。

### 阶段 A 私有候选部署结果

| 项目 | 结果 | 去敏证据与结论 |
|---|---|---|
| 构建与私有发布 | 通过 | 复用既有独立候选，保存并发布新的合成 POC 版本；未覆盖正式 URL |
| 访问策略 | 通过 | Sites 私有部署门禁验证为 Owner-only；1 名 Owner、0 个群组 |
| 数据与绑定 | 通过 | 运行环境变量 0 项；候选 hosting 的 D1/R2 均为 `null`；未绑定 KEK，未读取 iCloud |
| Worker 合成 POC 单测 | 通过 | 4 项 Worker 测试与 2 项前端测试通过；验证 Owner Header、合成归档、CSP 回环白名单和无浏览器持久化 |
| Owner 实际页面访问 | 阻塞 | 发布成功后，当前 in-app Browser 被 Cloudflare 外层安全页拦截；一次等待后重载仍相同，请求未进入应用 Worker，近 15 分钟 Worker error 事件为 0 |
| HTTPS 回环与 Worker S/M/L 实测 | 未执行 | 页面未进入应用，不绕过安全防护，不把未运行结果冒充为通过 |

阶段 A 仍为：**条件未通过 / 外部访问策略阻塞**。恢复条件是 Owner 在受支持浏览器中能正常打开同一私有候选页面；届时只运行页面内三个合成 POC，不需要重新绑定任何数据或存储。阶段 B-E 继续保持未启动。

### 2026-08-12 阶段 A 外部阻塞复核

| 项目 | 结果 | 去敏证据与结论 |
|---|---|---|
| Owner 页面复测 | 仍阻塞 | 同一候选页面再次命中 Cloudflare 外层安全页；未尝试绕过安全拦截 |
| 可用浏览器环境 | 受限 | 当前 Agent 环境只有 in-app Browser，未连接可供自动化验证的 Chrome 扩展 |
| Sites 项目状态 | 正常 | 项目为 active，当前身份为 Owner，访问策略为 custom；1 名允许用户、0 个群组、0 个外部访客 |
| 数据与绑定复核 | 通过 | 运行环境变量仍为 0；未读取 iCloud，也未新增 D1、R2 或 Secret |
| Worker 错误日志 | 无应用侧错误 | 最近 60 分钟错误事件为 0，支持“请求未进入应用 Worker”的判断 |
| CI | 通过 | PR #39 的 privacy、python、node 检查均通过；候选横幅测试已与当前实现对齐 |

复核后阶段 A 状态不变：**条件未通过 / 外部访问环境阻塞**。现有证据可以证明候选部署与应用配置正常，但不能证明 HTTPS 页面回环兼容性或 Worker S/M/L 平台容量。

### Gate 2A 调整（PO 已确认）

为避免把外部访问故障误判为产品实现失败，同时不跳过容量风险，提出以下受限调整：

1. 允许先实现与 Worker 平台容量无关的本机代理原子覆盖核心，以及系统页减法与纯合成 UI 状态；只运行本地、Miniflare 与合成测试。
2. Worker 单请求完整导出、真实 HTTPS 浏览器桥接和端到端联调继续阻塞，不因本地测试通过而宣称可上线。
3. 阶段 A 的浏览器回环与 Worker S/M/L 实测改为阶段 E/F 前的强制门禁；未通过不得进入私有验收、正式部署、真实备份或切源。
4. 本提案不包含生成 SIWC bypass token、创建新候选项目、重新部署、读取真实数据、删除资源或合并 PR。

PO 已于 2026-08-12 明确确认 Gate 2A。授权只覆盖本机代理原子覆盖核心、系统页减法和纯合成 UI；Worker 导出、HTTPS 回环、端到端联调、部署、真实数据、资源删除和 PR 合并继续保持阻塞。

### Gate 2A 首轮实现证据

| 项目 | 结果 | 去敏证据与结论 |
|---|---|---|
| 本机原子覆盖核心 | 通过 | 新增无凭据、无默认个人路径的 `life-console-backup/1` 校验与原子替换模块；只接收调用方提供的二进制流和显式本地路径 |
| ZIP 安全边界 | 通过 | 拒绝绝对/穿越路径、Windows 风格路径、符号链接、重复路径、未声明文件、单文件/总量/压缩比超限、摘要或数量不一致 |
| 旧备份保护 | 通过 | 临时文件权限 `0600`、流式写入、`fsync`、两次校验与 `os.replace`；写入中断和摘要错误测试证明旧备份字节不变 |
| 并发与回执 | 通过 | 单进程文件锁、运行 ID 冲突关闭、同运行幂等和去敏待回执增删测试通过；回执不保存正文或机器路径 |
| Python 聚焦测试 | 通过 | 本机代理核心 7 项测试全部通过，均使用合成 ZIP 和临时目录 |
| 系统页减法 | 通过 | Sites 系统页移除恢复包、R2 完整备份、冷备队列和审计摘要的请求与展示；普通界面不显示密钥版本或底层资源标识 |
| iCloud 最新备份 UI | 通过 | 单一备份卡覆盖 8 种纯合成状态、失败保护文案和桌面代理未连接说明；当前不调用 Worker archive API |
| 前端聚焦验证 | 通过 | 4 个 UI 测试文件共 11 项通过，TypeScript/Vite 生产构建通过 |
| Life Console 全量回归 | 通过 | 在允许本机回环绑定的受控测试环境中，129 项 Vitest、90 项 Python 测试及生产构建全部通过 |
| 项目级 Python 回归 | 通过 | 333 项通过、1 项因私有自动化契约不入 Git 而按设计跳过 |

Gate 2A 首轮实现不改变阶段 A 的总状态：**HTTPS 页面回环与 Worker S/M/L 平台容量仍未验证**。因此 Worker 导出、真实浏览器桥接、端到端联调和任何部署仍不得启动或宣称完成。

### Gate 2B 替代验收评审（PO 已确认）

#### 评审结论

多角色一致建议：**保留当前产品架构，先把自动化浏览器验收替换为普通 Chrome 人工 POC；不因扩展不可安装直接改成下载后导入。**

| 角色 | 结论 | 评审意见 |
|---|---|---|
| PO | 已确认 | 人工 POC 可作为阶段 A 浏览器证据；失败后才进入二级降级评审 |
| PMO | 建议通过 | 变更仅限验收方法，不扩大 2.1.0 产品范围 |
| 前端 | 建议通过 | 候选页已有三项合成测试，扩展不参与页面网络请求 |
| 后端 | 建议通过 | Worker S/M/L 仍由同一候选 Worker 执行，不降低平台容量门槛 |
| QA | 有条件通过 | 必须生成结构化去敏回执，不能只依赖口头“已点击” |
| 安全/隐私 | 有条件通过 | 回执不得含 UA/IP/资源标识/路径/凭据；不得绕过安全拦截 |

#### 人工 POC 验收步骤

1. Agent 在本机启动纯合成回环接收器；目标仍为临时目录，不读取 iCloud。
2. Owner 使用普通 Chrome 打开既有 Owner-only 候选页；不要求安装扩展。
3. Owner 依次运行回环连接、合成 ZIP 转发和 Worker S/M/L。
4. 页面生成去敏回执；Owner 将回执文件作为本地证据交给 Agent，或复制不含敏感信息的回执内容。
5. Agent 校验格式、三项状态和 S/M/L 数据完整性，再把去敏结论写入本文件。

通过标准不变：回环连接与合成传输均为 `passed`；S/M/L 均完成且 L 档不接近平台失败边界；本机旧备份保护仍由既有原子性测试证明。任何一项失败都保持阶段 A 阻塞。

#### 当前边界

- PO 已确认本节方案、阶段 2B-1 去敏回执通用代码与合成测试，并于 2026-08-12 单独授权复用既有私有候选项目部署合成 POC 构建；仍不授权正式部署、Worker 正式导出实现或真实数据访问。
- 候选构建已部署；普通 Chrome 访问及三项人工 POC 仍需实测。扩展商店错误和 in-app Browser 外层拦截都不能代替普通 Chrome 的实际结果。
- 若普通 Chrome 仍被 Cloudflare 拦截，优先处理候选测试环境，不创建重复 Sites 项目。
- 若页面可访问但 HTTPS → localhost 失败，再回到 Gate 2B 评审“下载后本机导入”，不得静默降低为不校验的浏览器下载。

#### 阶段 2B-1 本地实现证据

| 项目 | 结果 | 去敏结论 |
|---|---|---|
| 回执白名单 | 通过 | 固定格式仅包含 `synthetic`、人工 Chrome 模式、三项状态、S/M/L 指标与 UTC 生成时间 |
| 回执防泄漏 | 通过 | 不接受或生成 Cookie、Token、UA、IP、主机名、机器路径、Sites/资源标识、错误堆栈或个人数据字段 |
| 导出门禁 | 通过 | 回环、传输、容量三项都结束后按钮才启用；失败状态也可如实导出，不把失败隐藏为不可取证 |
| 本地导出 | 通过 | 用户主动下载 JSON；页面不自动上传，创建下载后释放临时 Blob URL |
| 聚焦测试与构建 | 通过 | 8 项阶段 A/2B-1 合成测试通过，未部署候选构建通过 |
| 全量回归 | 通过 | 131 项 Vitest、90 项 Life Console Python、333 项项目级 Python 测试以及生产构建通过；1 项私有自动化契约按 Git 边界跳过 |

本地实现和候选部署完成都不等于阶段 A POC 通过。普通 Chrome 实际页面、HTTPS 回环和 Worker S/M/L 仍需由 Owner 手工执行并导出去敏回执。

#### 候选部署后验收证据（2026-08-12）

| 项目 | 结果 | 去敏结论 |
|---|---|---|
| 部署目标 | 通过 | 复用既有独立私有候选项目；未创建重复项目、未覆盖正式 URL |
| 源码与构建 | 通过 | 已推送当前 PR 分支的精确已验证提交，并保存、私密发布 13 文件的合成候选归档 |
| 访问策略 | 通过 | 项目 active，当前身份为 Owner，`custom` 白名单仅 1 名允许用户、0 群组、0 外部访客 |
| 未登录拒绝 | 通过 | 无登录态只读请求实际返回 401；未把 401 记成非 Owner 403 |
| 非 Owner 403 | 延期 | 当前仍无第二个不在白名单中的已登录账号 |
| 资源边界 | 通过 | 0 环境变量、0 D1、0 R2；未绑定 KEK、未读取 iCloud、未创建真实记录 |
| Owner 页面实测 | 阻塞 | Sites 读回当前用户为 Owner 且新版本已上线，但 in-app Browser 仍停在 Cloudflare 外层安全页，未到达应用 |
| 普通 Chrome POC | 待执行 | 仍需 Owner 在普通 Chrome 运行回环、合成 ZIP 转发和 Worker S/M/L，并下载去敏回执后方可判定阶段 A |

## 4. Worker 与 D1 测试用例

| ID | 用例 | 通过标准 |
|---|---|---|
| WK-01 | 创建备份运行 | 仅 Owner + CSRF 可创建；初始 `CREATED` |
| WK-02 | 非 Owner / 未登录 | 分别按实际身份门禁返回拒绝；不把 401 冒充 403 |
| WK-03 | 一致导出 | 各个人资源数量、ID、revision 和摘要与合成源一致 |
| WK-04 | 敏感字段解密 | ZIP 内是规范化恢复字段；D1 仍存密文 |
| WK-05 | 排除运维数据 | ZIP 不含 audit、idempotency、backup_runs、会话和 hash |
| WK-06 | 写入锁 | 导出期间写请求返回 `423 backup_in_progress`，读取仍可用 |
| WK-07 | 锁超时 | 异常后 TTL 释放，不造成永久只读 |
| WK-08 | 状态转换 | 只允许定义的顺序；非法 complete/fail 返回冲突 |
| WK-09 | 去敏失败 | `error_code` 仅固定枚举，不保存异常正文 |
| WK-10 | 审计最小化 | 记录 backup 动作与结果，不含 ZIP、正文、查询参数 |
| WK-11 | R2 独立 | 新备份链路在无 R2 binding 时仍完整通过 |
| WK-12 | 恢复包路由移除 | 三个恢复包路由不可用，契约与前端均无引用 |

## 5. 备份格式与恢复契约测试

| ID | 用例 | 通过标准 |
|---|---|---|
| FM-01 | manifest 完整 | 格式、schema、导出 ID、时间、资源路径、数量、摘要齐全 |
| FM-02 | 规范化输出 | 同一数据输入产生相同内容摘要，时间/运行 ID 字段按约定排除 |
| FM-03 | 关联完整 | journal revisions、health segments 的父子 ID 都可解析 |
| FM-04 | 删除状态 | 软删除与删除计划保留；已硬删除正文不复活 |
| FM-05 | 空数据 | 所有文件存在、数量 0、摘要有效，可通过校验 |
| FM-06 | 字符与时间 | 中文、emoji、换行、UTC/本地日期不丢失 |
| FM-07 | 独立验证 | 在无 D1、无 KEK、无网络环境下可验证 ZIP |
| FM-08 | 恢复计划 | 工具可输出待建表、记录数量和冲突风险，但不自动写入真实项目 |

2.1.0 只要求提供 `verify` 和 `restore-plan` 契约；真正 `restore` 命令或在线 UI 属于后续独立需求与 PO 门禁。

## 6. 本机代理原子性与安全测试

| ID | 用例 | 通过标准 |
|---|---|---|
| LA-01 | 首次成功 | 生成唯一最新 ZIP，文件权限 `0600`，父目录收敛 |
| LA-02 | 成功覆盖 | 新包校验通过后原子替换；最终只保留一份稳定备份 |
| LA-03 | 写入中断 | 临时文件清理，旧备份字节完全不变 |
| LA-04 | 空间不足 | 返回固定错误码，旧备份不变 |
| LA-05 | 摘要错误 | 拒绝替换，旧备份不变 |
| LA-06 | ZIP Slip | 绝对路径、`..`、符号链接、重复路径全部拒绝 |
| LA-07 | Zip bomb | 文件数、单文件、总解压大小和压缩比超限时拒绝 |
| LA-08 | 非法 Origin | 不返回 CORS 授权，不写临时文件 |
| LA-09 | 并发请求 | 只允许一个运行；第二个返回忙碌，不互相覆盖 |
| LA-10 | 重复运行 ID | 幂等返回既有回执，不重复替换 |
| LA-11 | 回执补报 | 页面关闭后只保留去敏回执；补报后可删除 |
| LA-12 | 日志扫描 | 不出现个人正文、ZIP、完整摘要、凭据或机器路径 |

## 7. 前端测试

| ID | 用例 | 通过标准 |
|---|---|---|
| UI-01 | 系统页减法 | 恢复包、R2 下载、审计摘要、逐条队列均不存在 |
| UI-02 | 单一备份卡 | 最近成功、范围、代理状态和唯一按钮正确 |
| UI-03 | 代理离线 | 不请求云端 archive，显示连接方法 |
| UI-04 | 权限拒绝 | 给出可行动说明，不循环弹权限 |
| UI-05 | 状态序列 | READY → PREPARING → TRANSFERRING → VERIFYING → SUCCESS |
| UI-06 | 失败优先说明 | 先说明旧备份安全，再给重试/处理方法 |
| UI-07 | 页面关闭提醒 | 仅运行中提示；成功或未开始不阻止关闭 |
| UI-08 | 移动端 | 只显示状态，“请在已连接的 Mac 上备份” |
| UI-09 | 数据不持久化 | ZIP/正文不进入 localStorage、sessionStorage、状态快照或错误上报 |
| UI-10 | 无障碍 | 键盘、焦点、按钮禁用和 `aria-live` 通过 |

## 8. 合成 E2E 主链路

```mermaid
flowchart LR
    A[合成 D1 + 合成 KEK] --> B[Owner 浏览器创建备份]
    B --> C[Worker 生成规范化 ZIP]
    C --> D[浏览器转发到回环代理]
    D --> E[临时目录原子替换]
    E --> F[代理读回校验]
    F --> G[浏览器回报 SUCCESS]
    G --> H[系统页显示最近成功]
```

必须增加三个故障 E2E：

1. 传输 50% 时终止代理，旧备份不变。
2. 代理写成功后阻断云端回报，下次打开页面补报成功。
3. 导出期间模拟写请求，前端保留草稿并在备份结束后可重试。

## 9. 性能与容量门槛

Gate 2 后用纯合成数据建立三个档位，数量不映射真实用户数据：

| 档位 | 用途 | 门槛 |
|---|---|---|
| S | CI 日常 | 全链路快速完成 |
| M | 预估日常上限的合成档 | Worker 不超时、不超内存，浏览器无明显卡死 |
| L | M 的 2 倍 | 用于决定单请求 ZIP 是否可实施 |

若 L 档失败或接近平台限制，技术结论变为阻塞：停止实现单请求方案，重新设计版本化分块和一致性，不以降低测试规模掩盖风险。

## 10. 阶段实施与验收

| 阶段 | 工作块 | 验收产物 | PO 门禁 |
|---|---|---|---|
| A | 回环与容量 POC（≤2h） | 合成 POC 报告、浏览器兼容结论 | Gate 2 后可执行 |
| B | Worker 导出与格式（≤3h） | API、migration、格式验证、单元/集成测试 | POC 通过 |
| C | 本机代理原子覆盖（≤3h） | 临时目录测试、故障注入、安全测试 | POC 通过 |
| D | 系统页减法与桥接（≤3h） | UI 测试、前端桥接、无恢复包/审计证明 | B/C |
| E | 合成联调（≤3h） | Miniflare + Playwright E2E 与故障证据 | B/C/D |
| F | 私有候选验收（≤2h） | 无真实数据候选、PO 视觉/交互验收 | 独立部署授权 |
| G | 正式上线与真实备份 | 正式部署、真实 iCloud 首次备份 | 每项分别 PO 确认 |

任何阶段失败都停在当前工作块，不自动扩大到正式部署、真实数据或资源清理。

## 11. 合入前检查

```bash
python3 tools/check_project_governance.py
python3 tools/validate_project.py
tools/check_git_privacy.sh
tools/check_git_privacy.sh --history origin/main..HEAD
git diff --check
python3 -m unittest discover -s tools -p 'test_*.py'
cd apps/life-console
npm test
npm run build
```

2.1.0 实现后还必须增加并运行明确的 Worker、回环 POC、Miniflare 和 Playwright 命令；命令名以实现 PR 的 `package.json`/工具帮助为准，不在方案阶段虚构通过数量。

## 12. 当前已确认与未确认

### 已确认

- Gate 1、PRD draft.1、D1-D8。
- Gate 2 的 O1-O5、Q1-Q7、测试方案及“先回环与容量 POC、后实现”的阶段计划。
- 阶段 A-E 的通用代码与合成测试；PR #39 是唯一活动分支，#38 提炼验证后关闭。
- Gate 2A：允许提前实现本机代理原子覆盖核心与系统页纯合成 UI；平台相关门禁不前移。

### 尚未确认或尚未验证

- 回环接口在当前 in-app Browser/Chrome 的实测兼容性。
- 单请求 ZIP 的容量与平台上限。
- 普通 Chrome 人工 POC 的实际运行结果；去敏回执格式及二级降级边界已确认并完成本地实现。
- 正式部署、真实数据读取或备份、真相源切换、资源删除及 PR 合并。

## 13. PO Gate 2 确认记录

- 当前状态：PO 已确认通过。
- 确认对象：设计、技术、测试方案及先 POC 后实现的阶段计划。
- Gate 2 结论：O1-O5、Q1-Q7 与测试方案通过，只授权阶段 A-E 的通用代码与合成测试。
- 正式部署、真实 iCloud 首次备份、真相源切换、R2/Secret 清理和 PR 合并继续分别请求确认。
