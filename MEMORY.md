# 长期记忆

## 当前设计决定

### 2026-08-03：采用私有 GitHub 根仓库进行多 Agent 协作

- 使用私有仓库 `Matthew-1996/life-assistant` 统一管理项目代码、规则、测试、必要用户上下文和网页源码；原 `web/life-dashboard/` 独立仓库已并入根仓库。
- GitHub 不是个人生活数据真相源。日记原文与索引、每日/每周/阶段台账、Apple Health 文件、备份、导出、同步收据和外部服务标识由根级 `.gitignore` 排除，继续只按各自规则保存在 iCloud 项目或外部私有展示层。
- 网页旧仓库的完整历史已在 `backups/git-history/` 保存为校验通过的 bundle，并保留原 Git 元数据副本；该目录不进入新仓库。
- 多 Agent 并行修改使用独立分支和 `git worktree`，通过 Pull Request 合并，不让多个 Agent 共用同一个工作目录，也不强制推送共享分支。具体约定见 `GIT_WORKFLOW.md`。

### 2026-07-31：生活助手第一版路线

- 先建设用户建档、可纠正记忆、动态目标维护、今日规划、低打扰简报、每周复盘和行动审批七个闭环。
- 健康、饮食、财务、关系、家庭和出行能力在底座稳定后按真实需求增加。
- 公开社区 Skill 只作为设计参考；未经源码、依赖、权限和运行时审计，不直接安装。
- 主动提醒采用“重要、及时、可信、可行动”四项阈值，并尊重尚待确认的安静时段。
- 习惯与拖延支持先区分能力、机会和动机；目标由用户选择，之后才使用 if–then 计划、最低版本和中断恢复规则。

### 2026-07-31：采用动态生活目标台账

- 使用 `GOALS.md` 维护当前重点、辅助、候选、暂停和完成目标。
- 当前重点不视为永久第一目标；新目标加入或到达复盘日期时重新排序，不把所有目标继续叠加。
- 已确认的当前重点是改善睡眠质量与整体生活体验，首次复盘日期为 2026-08-14。
- 临时作息、工作过渡状态和一次性安全状态不沉淀为长期用户事实。

### 2026-08-01：采用四小时规划上限与主次排序

- 长期方向可以跨周或跨阶段，但单个可执行目标或连续工作块原则上不超过 4 小时。
- 预计超过 4 小时时先把它视为范围或不确定性异常，拆成可独立验收的阶段并与用户商量，不把超长任务静默塞进计划。
- 计划先标出唯一主项，再区分次项与可延后、合并或不做项；新增事项不能挤掉当前主项而不说明取舍。

### 2026-07-31：项目必须可随 iCloud 独立迁移

- 项目目录是背景、规则、目标、研究、Prompt 和验证工具的可迁移来源。
- 不依赖旧机器的用户缓存、绝对路径、Python 虚拟环境或系统级 Skill 文件。
- 原始聊天记录不作为唯一上下文；关键背景与决定沉淀到 `PROJECT_CONTEXT.md`、`USER.md`、`MEMORY.md` 和 `GOALS.md`。
- 迁移说明、零依赖验证器和压缩快照都保存在项目目录内；秘密和访问令牌不进入备份。

### 2026-08-01：采用可视化生活计划工作簿

- 使用 `outputs/019fb832-be4f-74f1-add5-58cb6fb6fc09/生活计划表.xlsx` 维护阶段路线、两周行动、每日记录和每周复盘。
- `GOALS.md` 继续作为目标状态与优先级的真相来源；工作簿负责可视化和记录，不自行改变目标排序。
- “每日记录”D:P 和“每周复盘”I:N 分别是每日、周复盘源台账的纯派生视图；缺失源数据保持空白，不从旧单元格、日记或助手推断补齐。
- 当前只承诺 2026-08-01 至 2026-08-14 的睡眠与生活恢复阶段；之后的交接保护、离职后减压、生活结构重建和下一重点选择均为待复盘确认的建议。
- 维护节奏默认是每天最多 1 分钟可选记录、每周一个小实验、阶段复盘时才调整目标；空白不视为失败，也不要求补记录。

### 2026-08-03：切换为 iCloud 真相源与 Google 表格展示层

- 用户明确选择个人 Google 账号中的私有原生 Google 表格作为生活计划的主要多端展示；当前 iCloud 项目中的 `GOALS.md`、`journal/` 与 `records/` 仍是唯一真相源，Google 表格只读、单向派生，不反向写回。
- 允许同步现有八个生活计划视图及轻量日记索引；日记原文、苹果健康摘要/睡眠明细、Prompt、凭据和聊天原文仍不得上传。Google 是额外云副本，表格保持仅本人访问。
- 每次记录先完成本地原子写入并立即确认，再尝试刷新 Google 表格；同步失败不回滚本地数据，保留待刷新状态并在下次记录重试。现有 XLSX 保留为迁移快照与手工 Numbers 备选，不再作为日常派生真相；`@oai/artifact-tool` 仅在重新导出 XLSX 时才是可选依赖。
- 可迁移配置位于 `integrations/google-sheets.json`；只保存策略、页面集合、表格 ID/链接和源哈希收据，不保存 Google 凭据。派生载荷由 `tools/google_sheets_payload.mjs` 从完整本地源生成，状态由 `tools/google_sheets_state.py` 管理。

### 2026-08-01：移动端只读看板与对话式记录

- 移动端网页源码位于 `web/life-dashboard/`，展示当前阶段、今日锚点、两周路径、阶段路线和回复模板；网页不收集表单数据。
- 用户通过当前对话回复，由助手负责解析并同步生活计划工作簿、目标台账和网页，避免让用户在大型文档中手动找位置录入。
- 已创建应用内主动回访：2026-08-02 至 2026-08-14 每天 11:15（Asia/Shanghai）运行；8 月 2 日虽为周日但作为首日使用一分钟普通回访，之后的周日改为自然周问答，8 月 14 日改为阶段复盘。周一另用 `review-plan` 检查已经结束、且有日记素材的自然周，避免周日白天提前整理而遗漏周日晚间记录。阶段复盘分别询问日记整理节奏与主动回访的帮助/打扰感；首轮结束后只有用户明确选择才继续或降频，否则保持安静。
- 自动化属于机器本地状态；可读说明、机器契约与规范提示词分别保存在 `automations/生活状态回访.md`、`automations/registry.json` 和 `automations/生活状态回访.prompt.txt`。
- 回访回复先按主要意图路由：泛化拒答、普通问题、提醒设置和删除/更正意图不写每日状态；显式日记即使偶然提到睡眠或情绪也默认只归档日记，只有另用回访格式或明确要求兼作状态时才分别写入。真实状态回答用 `tools/daily_checkin.py upsert` 按 Asia/Shanghai 日期写入 `records/daily-checkins.jsonl`；同日补充更新唯一记录，未提供字段不清空。每次成功结果（含 `unchanged`）后仍同步工作簿，避免用户重复填写。
- 生活计划表“每日记录”D:P 是每日状态台账的纯派生视图：同步前先校验日期、表头和公式，再只清日期行的内容并按完整台账重写；不从工作簿旧值反向补全。用户删除某日状态时先用 `purge-plan` 冻结 revision 与内容哈希，经当次精确确认后 `purge` 当前项目源记录并同步清空派生值；旧 ZIP、聊天与 iCloud/设备历史不随命令删除。
- 2026-08-02 首轮回访暴露了评分日期歧义：用户当次给出的睡眠质量、精力、情绪、生活实感都意在评分 2026-08-02，但旧规则将后三项归到了 2026-08-01。已清除 8 月 1 日的三项误归值并写回 8 月 2 日。今后普通上午回访必须显示具体日期；四项 1–5 分统一归回访当天，睡眠质量看今早结束的那一晚，精力/情绪/生活实感看截至回访时的当前状态；昨天只回顾生活动作与晚间降速。
- 2026-08-02 用户授权把苹果健康作为低频辅助数据源。每日回访只读睡眠与活动小摘要：缺失或无效时静默退回普通提问，主观评分和行动锚点不从设备数据推断。用户给出大约时间且与对应设备时刻相差不超过 1 小时时，采用设备精确值；用户给出精确值、否定设备或要求以自己为准时例外。差值超过 1 小时但不到 2 小时时保留用户值，达到 2 小时时先确认。本次用户粗略值 03:30/09:00 与设备 03:48/08:54 分别相差 18/6 分钟，已将 2026-08-02 更正为设备时刻。iPhone 快捷指令仍每天 10:55 先于 11:15 回访运行；睡眠明细仅按需分析或校验。
- 2026-08-03 进一步把睡眠校准改为逐字段确定性流程：六行摘要起止串日、跨度超过 18 小时或字段异常时，由 `tools/apple_health_sleep.py` 自动回退两日明细；近似用户时间与设备差 `≤60` 分钟采用设备，`61–119` 分钟保留用户，`≥120` 分钟先确认，用户明确精确或否定设备时始终以用户为准。设备 `sleep_end` 只进入最终醒来的 `wake_time`，离床只采用用户表达。每日台账升级为 schema v2，醒来成为主要睡眠结束字段；离床底层独立、日常按需记录。2026-08-03 已更正为 00:29 入睡、09:30 醒来、10:00 离床；2026-08-02 按用户决定不重新解释，只随迁移新增空的 `wake_time`。
- 2026-08-02 首次 11:15 回访未运行。用户随后回忆当天可能曾为了修正“已安排”的显示，手动把 UTC 规则的 `BYHOUR=3` 改成 `11`，这与漏发证据相符；只读核验时没有当天运行记录，12:24 配置已恢复为 `BYHOUR=3`，下一次指向 2026-08-03 11:15。Codex“已安排”显示的 03:15 是内部 UTC 值，上海本地时间为 11:15；应以“下一次运行”及数据库本地换算为准，不再手改内部小时。用户同日明确要求采用显示名补救，运行时名称改为“每天11:15（上海）｜生活状态回访与周复盘”，不改变 UTC 规则。
- 周日回顾固定覆盖当天所在的周一至周日自然周，不使用任意连续 7 天或不完整周；延迟回复仍写回原问题对应周，阶段复盘不冒充自然周记录。
- 用户在知情后明确同意将睡眠、精力、情绪、离职交接和恢复阶段信息私密发布到 OpenAI Sites。移动端看板已作为仅本人访问站点发布：`https://life-compass-cn-2026.ycy19821817850.chatgpt.site`；网页仍为只读，不收集对话回复。
- 移动端源码随后已改为按上海日期滚动展示连续七天，并把日期进度与阶段确认分开：当前只镜像 `GOALS.md` 已确认的 phase 01；2026-08-15 后若没有新决定，页面显示等待复盘并继续最低恢复锚点，不会按日期自动启动 phase 02。页面还补充日记更正、撤回、恢复和永久删除的准确边界；这些更新目前只在项目源码中，本轮未再次发布。过去的私密托管授权不替代再次发布所需的当次明确同意。
- `web/life-dashboard/PUBLICATION_STATE.json` 用不含站点标识的部署源码指纹维护“本地已验证”和“已发布”的区别。当前明确为 `local_changes_unpublished`；运行状态应显示 `ATTENTION` 而不是把存在托管元数据误报为线上已同步。只有托管成功后才可改成 `published_current`，状态工具仍不冒充联网访问核验。
- 2026-08-14 的第一条阶段复盘最多三个可跳过的问题组；日记整理节奏与主动回访节奏分别决定，漏答不默认继续。若用户选择 2026-08-31 再聊职业，必须先展示一次性提醒的日期、当地时间和目标，并取得当次明确确认后才创建运行时提醒。
- 2026-08-03 用户进一步优化普通回访：问题最下面必须附可复制的极简回复模板，并且在询问昨天的生活动作、晚间降速前，先用昨天有效日记的轻量索引总结可能相关的活动。日记总结只帮助回忆，必须让用户确认或补充后才能写入每日状态，不能从日记自动推断锚点。

### 2026-08-01：采用独立的对话式自然周复盘台账

- 使用 `records/weekly-reviews.jsonl` 保存用户对完整 ISO 自然周问题的明确短回答；它独立于日记与日记回顾、每日状态、目标台账和长期记忆，不复制原始对话，也不从其他来源反向生成用户答案。
- `tools/weekly_review.py` 只用 Python 标准库，通过 stdin 接受固定 JSON 子集；同周幂等 upsert、独立文件锁、revision、原子替换、严格 schema、摘要二次去敏和跨年 ISO 周验证共同保护可迁移数据。未回答字段为未知，完全相同的提交不改写文件。
- 周回答中的 `goal_intent` 只是意向，不自动改变 `GOALS.md`。泛化跳过、普通问题、提醒设置、显式日记和阶段复盘默认不进入周台账；只有明确要求兼作周回答时才按相应字段写入。
- 生活计划表“每周复盘”I:N 从完整周台账派生；无源记录时全空，部分回答只显示已提供字段并标为“部分复盘”。删除整周前先用 `purge-plan` 冻结 revision 与内容哈希，经当次精确确认后 `purge` 当前项目记录并同步清空派生值。
- 单周 purge 不删除聊天、`journal/`、每日状态、已另行确认的目标或记忆、网页、旧 ZIP 和 iCloud/设备历史；这些副本若需处理必须逐项识别和审批。能力建成时没有写入任何真实周记录，首次真实数据等待用户之后明确回答自然周复盘。

### 2026-08-01：扩展健身与职业探索候选轨道

- 用户希望逐步加入健身，但要等补充身体状态和目标后再细化；因此健身先进入候选目标，当前两周只保留轻活动，不预设高强度或完整训练处方。
- 2026-08-14 阶段复盘先在健身、职业或都不启用中选择；只有选中健身后才分批进行准备度对话，信息和恢复状态允许时再设计两周低量熟悉期。
- 新职业方向先进入候选目标。未选中时不设每周整理额度，只在自然想起时保存不含公司或第三方机密的去敏个人经验；选中后才用小实验验证方向，不以立即投递或一次性定终局作为默认要求。
- 健身与职业候选分支使用互斥开关：同一阶段至多启用一条，也可都不启用；未选中的分支不排期、不提醒，不会因日期到达自动开始。
- 生活活动采用轮换菜单而非叠加任务：低目的外出、关系连接、环境整理、兴趣或创作一次只选一个。

### 2026-08-01：采用对话式生活日记

- 用户可以直接用自然语言记录生活；以“日记”“日记记录”“生活记录”“记录一下”等词开头或明确要求留存时自动归档。明显且具体的亲身经历可以隐式归档，但必须在回执中说明并允许立即撤回；孤立状态、普通问题和工作排障不自动留存。
- 原始日记的结构化归档保存在项目内 `journal/`，默认隐私级别为 `local-only`；这里的含义是不由日记工具主动把原文发布到网页、工作簿或连接器，轻量索引仍会同步到同一 iCloud 项目的工作簿。它不等于离线或只有一份副本；对话产品保留、iCloud 同步和未独立加密 ZIP 的边界见 `journal/PRIVACY.md`。过去对睡眠、精力、情绪、离职交接和恢复阶段信息的私密发布授权，不自动扩展为发布全部日记。
- 日记原文按月存档。`journal/review-policy.json` 是整理节奏的可迁移真相：2026-08-02 至 2026-08-14 只启用已确认的试运行周整理，长期节奏保持 `pending_user_choice`；2026-08-14 等待用户在每周、每月、仅按需、暂停或暂不决定中明确选择，不因日期到达或漏答而默认续期。日记不计算连续天数，不作为绩效指标，也不自动改变目标排序。
- 日记与长期记忆分层。助手可以从细节中提出“候选认识”和规划线索，但写入 `USER.md`、`MEMORY.md` 或 `GOALS.md` 前必须展示摘要并由用户确认。
- 日记工具已形成可重复执行链路：`add` 保存原文与结构化索引，无法确认发生时间时使用 `time_precision=unknown`、只能估计时使用 `approximate`，不制造精确时间；普通新增采用快速路径，本地原子保存成功后立即短回执，不逐篇同步工作簿、刷新状态、完整校验或备份。工作簿派生索引与迁移级检查改在周/月回顾、用户查看或要求同步工作簿，以及备份、恢复、正式迁移或交接前批量完成。`amend` 留痕更正且在内容变化时要求完整重建轻量索引；`withdraw-latest-implicit` 按真实 `recorded_at` 原子撤回最近一次隐式保存；`withdraw`/`restore` 提供一般逻辑撤回与恢复；`purge` 和 `review` 仍保留完整保护与同步验证。旧 ZIP、聊天记录和 iCloud/设备版本不由 `purge` 自动删除。
- 日记机器索引采用必填字段与严格白名单，异常或原文字段 fail closed；`list` 只投影安全字段。含原文或更正说明的输入使用非 PTY stdin，避免进入 argv、stdout/stderr 或 PTY 回显日志。撤回/恢复状态只在稳定 ID 对应的 Markdown 条目块内修改，同月其他日记不串扰。
- 日记回顾中的候选长期认识和规划启示使用 `tools/journal_insights.py` 形成可迁移确认台账：每次最多展示 3 个，已接受/拒绝不重复询问，回顾漂移时旧候选失效。accept 只记录待执行，不自动修改 `USER.md`、`MEMORY.md` 或 `GOALS.md`；仍要展示拟写入文件的精确文字并经用户再次确认。
- 2026-08-14 等阶段复盘的明确回答使用 `records/phase-reviews.jsonl` 独立幂等保存；延迟回复仍写回原复盘日。它仅保存去敏摘要、严格枚举和布尔值，区分回访体验与后续频率；不自动应用目标、日记节奏或提醒。当前没有真实阶段复盘记录。
- 工作簿的未知事件时间保持空白，约略时间显示为“约 HH:MM”，精确时间保持可排序时间值；同步日志不打印标题、摘要、人物或周回答。四条合成记录的隔离端到端演练已经覆盖新增、更正、最近隐式撤回、同步、恢复、闭合周回顾和再次同步，并验证真实日记与正式工作簿未被测试污染。
- 永久删除中断恢复使用 schema v2：开始前冻结目标原文块、索引回顾引用与受管回顾的 SHA-256；有 pending 操作时阻止其他日记写入。若路径、内容、来源关系或索引发生漂移则停止删除，不自动扩大范围；恢复记录不保存标题或原文。
- `tools/journal_integrity.py` 双向核对机器索引与月度原文：索引中的每个稳定 ID 必须在约定来源恰好出现一次，原文标识也不能成为索引外孤儿。状态检查、项目验证和备份共享这条 fail-closed 约束，只输出计数和通用结构错误。备份直接核对固定的内存字节快照，并在发布前拒绝任何路径、身份、模式或内容漂移。

### 2026-08-01：结构验证与运行状态分层

- `tools/validate_project.py` 负责结构、引用、隐私和便携性底线；`tools/life_assistant_status.py` 负责目标节点、日记回顾、工作簿漂移、自动化契约与提示词完整性、当前设备自动化是否唯一且精确一致、移动端源码/发布记录关系和备份完整性等随时间变化的状态。运行时检查只输出状态与计数，不输出提示词、自动化或任务标识，也不能证明通知实际送达或站点实时可用。
- `STATUS.md` 是助手维护、随时可重建的派生快照，不是目标、记忆或日记的真相来源；为避免“状态变化触发备份、备份变化又触发状态”的循环，它不进入 ZIP 快照。
- 工作簿每次同步后都以 0600 权限原子生成 `生活计划表.sync-state.json`，精确绑定最终 XLSX 与日记/每日/每周三个源的存在性及字节 SHA-256；状态检查不再用 mtime 猜测同步。收据不含绝对路径、标题、摘要或原文。
- `ATTENTION` 只供助手在合适时机处理，不自动改变 `GOALS.md`、长期记忆或增加用户打卡；状态输出不复制日记原文，也不暴露站点项目标识。
- 2026-08-01 已把 `r18` 备份完整解压到新的临时目录进行恢复演练：三件套校验、93 个普通文件恢复、核心 doctor、项目验证、232 项 Python 测试、官方 Skill 校验、8 项移动端测试、日记到工作簿端到端演练、工作簿重新同步与 8 张工作表渲染均通过；同步收据状态为 current/matches，公式错误为 0。演练同时覆盖阶段复盘、候选认识、严格移动端发布状态与源码指纹检查，证据进入后续 `r19` 快照；Node、网页依赖、PyYAML 与 `@oai/artifact-tool` 仍按 `PORTABILITY.md` 在新环境重新定位，不写入备份。

## 已有产物

- 核心 Skill：`skills/improve-daily-life/SKILL.md`
- 动态生活目标台账：`GOALS.md`
- 两周睡眠与生活恢复计划：`plans/2026-07-31-两周睡眠与生活恢复计划.md`
- 睡眠与状态记录模板：`plans/睡眠与状态记录模板.md`
- 可视化生活计划工作簿：`outputs/019fb832-be4f-74f1-add5-58cb6fb6fc09/生活计划表.xlsx`
- 移动端生活看板源码：`web/life-dashboard/`
- 定时回访可读说明、机器契约与规范提示词：`automations/生活状态回访.md`、`automations/registry.json`、`automations/生活状态回访.prompt.txt`
- 研究报告：`research/2026-07-31-生活助手Skill与Prompt全网调研.md`
- 公开来源评分：`research/2026-07-31-公开来源评分表.md`
- 初始目标完成审计：`research/2026-07-31-初始目标完成审计.md`
- 对话式日记完成审计：`research/2026-08-01-对话式日记完成审计.md`
- 项目背景与交接摘要：`PROJECT_CONTEXT.md`
- 迁移与恢复说明：`PORTABILITY.md`
- 零依赖项目验证器：`tools/validate_project.py`
- 工作流 Prompt：`skills/improve-daily-life/references/workflow-prompts.md`
- 评估集：`skills/improve-daily-life/references/evals.md`
- 对话式日记：`journal/`
- 日记工作流：`skills/improve-daily-life/references/journaling.md`
- 日记隐私与删除边界：`journal/PRIVACY.md`
- 日记归档工具：`tools/journal_manager.py`
- 日记工作簿同步工具：`tools/update_life_plan_journal.mjs`
- 自然周复盘台账与工具：`records/weekly-reviews.jsonl`（无真实记录时可不存在）、`tools/weekly_review.py`
- 生活助手运行状态检查器及测试：`tools/life_assistant_status.py`、`tools/test_life_assistant_status.py`
- 可重建运行状态快照：`STATUS.md`

## 未完成

- 等待 2026-08-14 对首个两周恢复实验进行复盘，或按用户新信息提前调整。
- 等待用户在 2026-08-14 选择健身、职业或都不启用；只在选中健身后才分批补充身体状态、运动经历、目标、偏好与现实时间。
- 2026-08-31 只复盘 8 月 14 日已明确启用的分支，或由用户重新选择；不因日期到达自动启动职业探索。
- 尚未连接个人日历、任务、邮件、健康或财务数据。
- 尚未获得真实试运行结果。
- 尚无真实自然周复盘记录；空白不视为失败，也不要求补写。
- 尚未由用户选择 2026-08-14 之后的日记整理节奏；首轮周一回补能力不等于长期自动化已经续期。
- 移动端看板的最新滚动日期与日记控制文案只在本地源码中，尚未重新发布。
