# 项目背景与交接摘要

更新日期：2026-08-01

这份文件用于在聊天记录或旧电脑不可用时恢复项目背景。它只保存会影响未来协助的摘要，不保存密码、令牌、第三方秘密或完整对话逐字稿。

## 项目起点

用户希望建立一个真正有助于生活质量的生活助手 Agent。助手的价值不是增加待办，而是减少认知负担，帮助用户在健康、时间、关系、生活环境、财务安全、成长与乐趣之间形成更现实的平衡。

第一阶段任务是系统检索公开网络中的 Agent Skill、Prompt、架构、记忆、安全、行为支持和生活质量框架，并形成可审计的本地实现，而不是直接安装未知社区 Skill。

## 已形成的核心设计

生活助手采用一个主助手和八个闭环：

1. 最小化用户建档与权限；
2. 可查看、可纠正、可删除的记忆；
3. 可新增、排序、暂停、完成和替换的动态目标；
4. 合并日程、任务、精力和缓冲的现实规划；
5. 只在重要、及时、可信、可行动时出现的简报；
6. 从真实事实中选择一个实验的复盘；
7. 对外部、资金、隐私、健康和难恢复动作的分级审批。
8. 从自然对话留存生活经历、定期回顾但不自动固化为长期记忆的日记闭环。

核心实现位于 `skills/improve-daily-life/`。详细研究、来源评级和完成证据位于 `research/`。

## 真实使用与迭代

用户选择“改善睡眠质量与整体生活体验”作为第一个当前重点。该重点不是永久第一目标；目标状态和复盘规则见 `GOALS.md`。第一次真实使用生成了两周恢复计划和简洁记录模板，并暴露出两个需要产品化的能力：动态目标维护、睡眠与恢复专用工作流。两项都已进入核心 Skill、Prompt 包和评估集。

当前恢复计划是一项一般行为支持实验，不是诊断或治疗。健康问题持续、影响功能或出现危险信号时，应转向专业评估；药物和高影响健康动作不由助手自主决定。

## 当前生活与迁移背景（带日期，会过期）

以下是用户在 2026-07-31 明确提供、且对当前协助有直接影响的阶段性背景。后续应询问变化，不把它们当作永久身份或状态：

- 用户当时在字节跳动抖音互联网商业化广告团队担任产品经理，主要负责中国区品牌广告产品；已经正式提出离职，处于最后约一个月的交接期。
- 高工作压力曾伴随频繁失眠和情绪低落；用户当时明确表示没有达到“不想活”或伤害自己的程度。后续安全判断必须以用户的最新陈述为准。
- 当时通常凌晨 1–2 点入睡；工作日 9:30 闹钟、约 10:00 起床，周末约 11:00–12:00 起床。当前实验的具体调整见两周恢复计划。
- 当前使用的是需在约 2026 年 8 月底归还的工作电脑。项目目录位于 iCloud Drive；只有写入这个项目目录并完成同步的内容可视为能带走，机器缓存、系统安装和未导出的聊天不能视为备份。

## 信息与权限边界

- `USER.md` 保存经确认的稳定偏好和长期目标。
- `MEMORY.md` 保存重要设计决定、长期事实和验证结论。
- `GOALS.md` 是动态生活目标的当前来源。
- `records/daily-checkins.jsonl` 保存每日结构化状态；`records/weekly-reviews.jsonl` 保存完整自然周的明确回答；`records/phase-reviews.jsonl` 保存固定复盘日的明确回答；可选的 `records/phase-actions.jsonl` 只保存由阶段回答派生的期望值与执行状态。`journal/` 保存日记原文、索引、派生回顾和可选的候选确认台账，其中可包含尚未应用的精确长期文件提案。各层彼此独立，均不自动升级为长期目标或记忆。
- 临时作息、一次性情绪和短期工作状态不自动进入长期用户资料。
- 密码、令牌、完整证件或银行卡信息以及第三方秘密永不进入项目。
- 发送消息、影响他人的日程、消费、订阅、删除、公开发布、敏感数据共享及医疗/财务高影响动作必须取得当次明确同意。

## 当前状态

- 全网调研、核心 Skill、主 Prompt、14 个工作流 Prompt 和评估集已经完成。
- 核心 Skill 已通过官方 `quick_validate.py` 和项目内更广的结构检查。
- 可视化生活计划已改为两层结构：当前 iCloud 项目中的目标、日记与状态台账是唯一真相源；个人账号下的私密 Google 表格是只读派生展示层，保留原八个页面。`tools/google_sheets_payload.mjs` 从 `journal/index.jsonl`、`records/daily-checkins.jsonl` 和可选的 `records/weekly-reviews.jsonl` 生成确定性清空、写入和读回范围；“每日记录”D:P 保留入睡、醒来、离床三个独立字段，“每周复盘”I:N 只来自用户明确周回答，日记只同步轻量索引。日记原文、苹果健康摘要/明细、Prompt、凭据和聊天原文不进入 Google。每次记录先原子写 iCloud，再尝试刷新 Google；外部失败不回滚本地结果，并由下一次记录重试。`tools/google_sheets_state.py` 以源字节哈希和载荷哈希保存 0600 成功收据。原 `生活计划表.xlsx` 保留为迁移前快照和手工 Numbers 备选，不再日常同步或充当真相源。
- 已建立移动端只读生活看板和 2026-08-02 至 2026-08-14 的对话式主动回访；用户只需通过对话回复。回复先按主要意图路由：泛化拒答、普通问题、提醒设置和删除/更正不写每日状态；显式日记即使偶然提到睡眠或情绪也默认只归档日记，只有另用回访格式或明确要求兼作状态时才分别写入；真实状态字段由 `tools/daily_checkin.py` 按当地日期同日 upsert 到 iCloud 台账，再按非阻塞规则刷新 Google 展示层。8 月 2 日首日虽为周日仍使用普通一分钟回访；之后的周日问题固定覆盖提问日所在的周一至周日完整 ISO 自然周，用户明确回答的周字段通过 stdin 由 `tools/weekly_review.py` 同周 upsert 到独立台账，延迟回复也写回原问题对应周。日记回顾与这份用户周回答分开：周一使用 `review-plan` 检查已经结束且有素材的自然周，避免周日提前整理遗漏晚间记录。阶段复盘不冒充自然周记录，周回答缺项保持未知，`goal_intent` 不自动改变目标台账。8 月 14 日分别询问日记整理节奏与主动回访是否有帮助或打扰，用户未明确选择时不默认续期。用户已明确批准敏感生活信息的私密外部托管，站点地址为 `https://life-compass-cn-2026.ycy19821817850.chatgpt.site`。
- 移动端源码已采用上海当天起连续七日，并把日期进度与阶段确认分开：当前只确认 `GOALS.md` 中的 phase 01；8 月 15 日后若无新决定，显示等待复盘并继续最低恢复锚点，不会按日期自动启动后续建议阶段。日记更正/撤回/恢复/删除边界也已更新并通过 8 项本地构建测试；线上版本仍保持此前发布状态。任何再次发布都需取得当次明确同意，不能把过去的托管授权当作持续发布授权。
- `web/life-dashboard/PUBLICATION_STATE.json` 用部署相关源码的确定性指纹记录本地与发布版本的关系，不含站点标识或凭据。当前状态为 `local_changes_unpublished`；生活助手状态检查必须把它显示为待同步，而不能仅因 `.openai/hosting.json` 存在就报告线上已是新版。该状态仍不等于联网访问权限核验。
- 已建立项目内对话式生活日记：明确日记触发词会自动归档，具体生活叙述可在明确告知和可撤回的前提下隐式归档；网页和工作簿不复制原文。无法确认发生时间时保存为未知、只能估计时标为约值，不制造精确时间；内容更正必须完整重建轻量索引，最近一次隐式保存可按 `recorded_at` 原子撤回。机器索引采用严格字段白名单、列表只返回安全投影，含原文输入走非 PTY stdin；撤回或恢复只修改目标条目块，同月记录不串扰。`review-plan` 只列出已经结束且待整理的自然周/月、完整有效来源 ID 和 `source_set_etag`，`review` 在锁内重验完整来源集合后才幂等生成回顾；若计划后补记、撤回或更正则不写入。`journal/review-policy.json` 固定 2026-08-02 至 2026-08-14 的试运行周整理，长期每周、每月、仅按需或暂停仍待用户选择，未选择时不续期。`tools/journal_integrity.py`、项目验证、状态检查与备份都双向核对索引和月度原文，孤立来源或错配 fail closed。候选确认台账的状态链是 `pending → awaiting_proposal → proposed → applied`，拒绝为 `rejected`，来源漂移为 `superseded`；提案保存目标、精确文字与哈希，中断后可由只读 `apply-plan` 恢复。候选工具不直接写长期文件；助手只在用户对精确变更再确认后写入，`mark-applied` 又会核验目标字节已包含同一提案。revision/etag 防陈旧，公开状态只返回计数。永久删除恢复契约会冻结目标原文块和受影响回顾的哈希，pending 期间阻止其他写入，漂移时停止而不扩大删除范围。准确数据边界见 `journal/PRIVACY.md`；长期记忆和目标变化仍需用户确认。

- 已建立独立阶段复盘台账 `tools/phase_review.py`：延迟回复仍写回原复盘日，只保存明确的去敏摘要/严格枚举/布尔值；`life_experience_signal` 只是可选生活补给摘要，缺失时不推断。`tools/phase_actions.py` 只从明确且非中性的目标、整理/回访节奏、下一分支等回答派生可恢复动作。一旦回答 `next_track`，它会互斥筛掉未选的职业时点或健身对话；若未回答该字段，另行明确的依赖项会各自独立派生，因此要锁定互斥分支应先保存 `next_track`。来源 etag 漂移会使旧动作 `superseded`，当前动作以 `pending/applied/failed/dismissed` 及 revision/etag 保留可恢复状态。该工具不执行目标、策略、提醒或网页变更。当前没有真实阶段复盘或动作记录。
- 四条合成记录的隔离端到端演练已覆盖新增（三种时间精度）、完整更正、按记录时间撤回最近隐式日记、工作簿同步、恢复、闭合周回顾与再次同步；CLI、机器索引和 XLSX 均无原文哨兵，真实日记和正式工作簿的哈希保持不变。正式工作簿未知时间为空、约略时间显示“约 HH:MM”，并已重新渲染 8 张工作表且公式错误为 0。
- 已建立独立的自然周回答工具 `tools/weekly_review.py`：只用 Python 标准库，短摘要经 stdin 输入，不在命令参数或结果中回显；稳定键、独立锁、真正 no-op、revision、原子替换和严格 schema 保障同周幂等及并发安全。单字段清除和整周 purge 都不声称处理日记、每日状态、目标、聊天或历史副本。截至 2026-08-01 只有能力、测试和空源契约，没有真实周复盘记录；缺失文件或空文件是正常状态。
- 已建立零依赖的助手运行状态检查：覆盖目标复盘日期、日记与回顾状态、工作簿同步漂移、自动化注册表与规范提示词完整性、当前设备自动化是否唯一且与契约精确一致、移动端源码/发布记录关系和备份完整性；检查不会在报告中提取、复制或输出日记原文、自动化提示词、自动化/任务标识、源码指纹或站点标识，也不联网。它不能证明提醒实际送达或核验站点实时访问权限。
- 已建立只读迁移环境 doctor，区分核心 Python 标准库链路、可选 PyYAML 官方校验、工作簿 `@oai/artifact-tool` 运行时和移动端 Node/npm 依赖。项目文件是可迁移真相来源，但不再宣称所有可选编辑与构建能力“零依赖”。
- 备份脚本每次都提示 ZIP 含个人项目数据且无独立密码；非空日记、每日/周/阶段复盘、候选提案或阶段动作台账会分别提示敏感边界。写入前对高置信凭据文件名和私钥内容做预检，固定文件集合、身份、模式、精确字节和哈希，再直接在这份快照上校验日记双向完整性、阶段复盘、候选提案和阶段动作结构；任一异常都在 ZIP 与两份校验清单组成的三件套发布前 fail closed，且不输出台账内容。`tools/verify_backup.py` 再使用同 stem 的三件套校验 ZIP SHA-256、CRC、安全路径、普通文件类型、成员集合和逐文件哈希，可选只解压到不存在的新目录。
- 2026-08-01 已对 `生活助手-完整备份-2026-08-01-r18.zip` 做真实恢复演练：安全解压 93 个普通文件到新的临时目录；恢复副本通过核心 doctor、项目验证、232 项 Python 测试、官方 Skill 校验、8 项移动端测试、严格发布状态/源码指纹检查和日记到工作簿端到端演练，并在重新定位当前设备运行时后重新同步及渲染 8 张工作表。工作簿与收据均为 0600，状态为 current/matches，公式错误为 0。演练证据进入后续 `r19` 快照；临时运行时、依赖缓存和旧机器绝对路径没有写入项目。
- 当前项目与标准 ZIP 都位于同一个 iCloud 项目：ZIP 能对抗单文件损坏或误改，但不是脱离同一账号、同一设备与 iCloud 的独立故障域，也没有独立密码。是否建立另一份加密、离机副本及其密钥保管方式，仍需用户另行决定，不由助手静默执行。
- 用户已要求把循序渐进的健身和下一阶段职业规划纳入长期路线。两者暂为互斥候选分支：2026-08-14 只选健身、职业或都不启用，同一阶段至多启用一条；未选中的分支不排期、不提醒，2026-08-31 只复盘已启用分支或重新选择。具体路线见 `plans/2026-08-01-生活扩展路线图.md`。
- 当前生活目标及复盘日期见 `GOALS.md`，不要把本文件中的历史描述当作永久优先级。
- 尚未连接日历、任务、邮件、健康或财务数据，也没有保存相关凭据。

## 新环境恢复顺序

1. 阅读 `AGENTS.md`；
2. 阅读 `USER.md`、`MEMORY.md` 和 `GOALS.md`；
3. 按当前请求加载 `skills/improve-daily-life/SKILL.md` 及最少相关 reference；
4. 运行 `python3 tools/portability_doctor.py`，区分核心恢复阻塞与可选运行时缺失；
5. 运行 `python3 tools/validate_project.py`；
6. 验证日记、周/阶段复盘、候选提案和阶段动作链路：`python3 -m unittest tools.test_journal_manager tools.test_journal_integrity tools.test_weekly_review tools.test_phase_review tools.test_journal_insights tools.test_phase_actions -v`；测试只使用临时目录，不会创建真实台账；
7. 运行 `python3 tools/life_assistant_status.py --write STATUS.md`；
8. 继续当前目标，或由用户新增、降级、暂停或替换目标。
